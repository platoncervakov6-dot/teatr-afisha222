// lib/providers/ticketland.js
export async function fetchFromTicketland({ city = "moscow" } = {}) {
  // TODO: подключить публичный API/партнёрку или парсер со страниц событий (JSON-LD).
  return [];
}
